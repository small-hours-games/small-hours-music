# Suno API key (required for importing tracks)
SUNO_API_KEY=your_api_key_here

# Domain for public URLs (optional — defaults to local IP)
DOMAIN=music.yourdomain.com

# Port to listen on (default: 3000)
PORT=3000
